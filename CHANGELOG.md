# 更新日志

本文件记录 Quartz 各版本的主要变化，最新版本在最上方。

## 0.1.31 — 2026-06-14

### 修复
- **手动输入 API Key 不再「敲一个字符就提示已接入」**：以前在 Key 输入框敲第一个字符，就会误报「已接入」并把输入框重置（焦点也丢了）。现在改为输入完成（失焦 / 回车）且 Key 长度完整时，才接入默认模型并提示一次；回车可直接提交并移走焦点。
- **代码块（INI 等）在 AI 回答时上下跳动**：代码外框（语言头栏 + 复制按钮）现在从第一帧就稳定套上，块高不再随围栏开/合反复变化；代码还在写时先不上色，写完再高亮。
- **Windows：设置面板右上角不再被窗口按钮遮挡**：打开设置时，弹窗右上角（关闭按钮）会被系统「最小化 / 最大化 / 关闭」盖住一部分——现在弹窗整体下移到按钮带下方，完整可见。
- **Windows：右上角计费提示不再被窗口按钮遮挡**：鼠标悬停右上角计费信息时，弹出的提示会被系统窗口按钮盖住一角——现在自动避让到按钮左侧。
- **未设名字时点左下角「设置名字」跳错页**：以前会打开「统计」页并把光标塞进搜索框；现在正确打开 设置 → 常规 并聚焦「你的名字」输入框。

### 改进
- **打开设置不再自动聚焦搜索框**：以前一开设置光标就落在「搜索设置」里，容易误输入；现在焦点落在弹窗本身，Esc 关闭、Tab 切换照常。
- **设置页滚动条更顺眼**：滚动条上下留出间距，置顶时圆角不再与弹窗右上角圆角重合。

## 0.1.30 — 2026-06-14

### 新增
- **新对话首页问候**：按时段显示「早上好 / 中午好 / 下午好 / 晚上好 / 夜深了」加上你的名字，配上 Quartz 水晶；在 设置 → 常规 → 个人 里设置名字与头像（头像可上传图片，留空就用名字首字母）。
- **统计页**（设置 → 统计）：
  - GitHub 式每日 token 用量图（绿色格点，今天固定在右上角、每列都是整 7 天，不再有半截的当前周）；可切换「1 个月 / 1 年」。
  - 右侧汇总磁贴：今日、近 30 天、本周（含环比上周涨跌）、活跃日均，均带 token 与花费（¥）。
  - **AI 使用小结**：让模型读你的对话标题，生成一份「你最近在关注什么」的小结，按需生成、结果缓存本地。
- **侧栏底部**改为「头像 + 名字 ｜ 设置」——点头像/名字进统计页。

### 改进
- **已归档对话**从侧栏移到 设置 → 数据，侧栏更清爽；删除对话仍是软删除、可随时恢复或彻底删除。
- 回答开始时的滚动锚定改到「问题与回答之间」的位置（距顶约 20%）：发很长的一段话时，回答不再被顶到屏幕外。
- 设置窗口加高，左栏所有栏目都能完整显示。

## 0.1.29 — 2026-06-13

### 改进
- **「引用」改到右键菜单**：以前在回答里选中文字会浮出一个「引用」小按钮，容易误碰、输入框里莫名多出一段引用块。现在去掉了这个划词浮按钮，改为**选中回答文字后右键 →「引用选中文字」**——主动触发才会把引用填进输入框，不再打扰。

## 0.1.28 — 2026-06-13

### 修复
- **复制按钮不再弄坏自己**：点消息下方「复制」时若正好点到图标，过去会把图标抹掉、且看不到反馈；现在稳定显示「已复制」，图标不受影响。
- **确认框里的回车不再总是「确定」**：以前无论焦点在「取消」还是「确定」上，回车都会执行确定——归档区「全部删除」顺手一个回车就可能误清空。现在回车只触发当前聚焦的按钮；且「全部删除」即便确认了，也能在弹出的提示里一键撤销。
- **粘贴不再被聊天「抢走」**：在设置面板、搜索框等其它输入框里粘贴图片/文件时，不再被静默塞进聊天输入框、也不再抢走焦点。
- **草稿与附件按对话隔离**：切换对话时，没发出的半截文字和已贴的图会留在原对话、切回即恢复，不会带到（甚至误发到）另一个对话。
- **生成过程中删消息更安全**：回答还在流式输出时，删除会提示「正在生成」，不再打乱对话记录。
- **双击改标题更稳妥**：现在只有按回车才提交，点到别处＝取消——手滑不再静默覆盖标题，也不会悄悄关掉该对话的自动命名。
- **空回复可重试**：模型偶尔什么都没返回时，现在显示一张带「重试」的卡片，而不是存一句「（没有返回内容）」占位文本。
- 修复拖文件时输入框四周的虚线提示偶尔卡住不消失。

### 改进
- **开关不再谎报状态**：联网、思考开关在不支持的模型上会变灰，不再亮着「已开」却其实没生效；用 DeepSeek 贴图也会即时提示不支持，不必等到发送才被拦。
- **发送键空内容时置灰**：输入框为空且没有附件时发送键变暗，不再像点了没反应的坏按钮。
- **速答（QuickBar）回答更快**：DeepSeek V4 等混合模型默认会先「思考」拖慢速答——现在 QuickBar 显式关闭思考，回答秒出；同时滚动条不再遮挡文字。
- **键盘与无障碍**：全应用加了键盘焦点框；设置与确认框支持 Tab 循环、打开即聚焦；对话列表可用 ↑↓ / 回车导航；截断的对话标题悬停可看全名；图标按钮补了无障碍标签，提示消息可被读屏播报。
- **更顺手的小改进**：⌘F / Ctrl+F 跳到搜索；中断后的「继续」按钮更醒目，停止时提示「再按一次 Esc 撤销发送」；图片查看器加了缩放提示并支持 +／−／0 键缩放；侧栏拖宽可双击复位；设置搜索无结果时有说明。

### 其他
- 长代码回答流式输出时更跟手（代码未写完不再反复重建高亮）；侧栏搜索加防抖；PDF 附件加 20MB 上限；数据页的 API Key 安全提示对比度提升；确认框的红色仅保留给导入/恢复/清空等不可逆操作；推理面板完全展开不再被高度截断。

## 0.1.27 — 2026-06-12

### 改进
- **公式渲染更可靠**：模型若把数学公式塞进没标语言的代码块，只要内容确实是 LaTeX（含 `\frac`、带花括号的上下标等），现在会自动识别并渲染成公式，而不是一个代码框；同时在对话请求里加了引导，让模型优先用 `$…$` / `$$…$$` 输出 LaTeX，少把公式写成纯文本或塞进代码围栏。
- **选中文字更清晰（深色模式）**：文本选中色从系统默认的蓝色改为高对比的水晶色——选中引用、次要说明等灰色文字时不再发糊看不清。

### 修复
- **代码块语言标签不再乱猜**：没标注语言的代码块此前可能被自动猜成「INI」「Perl」等错误语言；现在统一显示「code」，只有作者明确标了语言才显示对应名称。

## 0.1.26 — 2026-06-10

### 改进
- **悬浮提示加了延迟**：大多数按钮的 tooltip 改为鼠标停留约 0.38 秒后才弹出，划过时不再频繁闪烁；压缩按钮（上下文用量）、模型 pill 等一目了然的仍保持秒弹，模型完整 ID、费用明细等次要信息则延后到约 0.6 秒。
- **单轮对话不显示节点小地图**：只有一问一答时，左侧的节点导航条隐藏，从第二轮提问起才出现。

## 0.1.25 — 2026-06-10

### 新增 / 改进
- **图片查看器**：点开消息或附件里的图片，改为应用内的暗色查看器——滚轮缩放、双击复原、点空白或 Esc 关闭，不再弹出一个空白新窗口。
- **中断后可「继续」**：回答生成时按 Esc 停止，已写出的内容会保留；操作栏新增「继续」，从断点接着往下写（两段花费会合并计入这条回答）。
- **Esc 两段交互**：回答中第一次按 Esc 停止并保留半截内容；停下后再按一次 Esc 撤销这次发送，提问与附件退回输入框（仅在输入框为空、且这仍是最后一轮时生效，不会覆盖你新打的字）。
- **引用回答追问**：选中 AI 回答里的一段文字，会浮出「引用」按钮，一键以引用块填入输入框继续追问。
- **界面缩放可记忆**：视图菜单的 ⌘ +/- 缩放会被记住，重启后保持。
- **从自动备份恢复**：设置 → 数据 新增「从自动备份恢复」，选一份本地自动备份即可一键还原（自动备份不含 API Key，现有 Key 会保留）。
- **上下文用量提示**：「压缩前文」按钮上会显示当前对话大致的上下文 token 量，更直观地判断何时该压缩。
- **对话列表排序方式**：设置 → 常规 可选按「修改时间」（最近聊过的在上）或「添加时间」（最近新建的在上）排序。
- **发送后自动定位**：在某个对话里发新消息时，若它不在侧栏可视区，列表会平滑滚动到它处于距顶约 20% 的位置。
- **侧栏更清爽**：左侧对话项去掉了图标、时间与提示词标签，收成单行——只剩标题，鼠标悬停时才在右侧显出置顶 / 删除；已归档列表也去掉了图标。
- **速答浮条优化**：浮条里输入问题后回车，改为直接回答（可能已改过的）新问题并让输入框失焦；失焦后再按回车才接力进入 Quartz；空输入框按 ↑ 可唤回本次最近一次提的问题。
- **只发图片 / 文件时的命名**：当某轮只发了图片或文件、没有文字时，该节点标题改用模型的回答来生成；若开场第一条就只发图片 / 文件，整个对话标题也由回答生成。
- **不支持图片的模型预检**：给 DeepSeek 这类不支持视觉的模型发图片时，会在发送前提示更换模型，而不是直接报错。
- **宽表格可横向滚动**：模型输出列很多的表格不再撑破布局，可在消息内左右滚动查看。
- **生成时禁用「重新生成标题」**：AI 正在回答或压缩时，顶栏的重新生成标题按钮会变灰，避免与正文生成抢占。
- 提示词标签与输入框左下的提示词名过长时，改为右侧透明渐隐（替代省略号）；使用引导补充了 Esc 停止 / 撤销的说明。

### 修复
- **只发图片 / 文件的消息不再留空气泡**：没有文字、只带图片或文件的消息，下方不再多出一个小圆角空泡。
- **关掉更新提示不再被当成「跳过该版本」**：左下角更新提示的关闭只是本次不再打扰，设置里「检查更新」仍会照常显示并可更新（旧版误写的「已忽略」状态也一并失效）。
- **长对话流式更顺**：回答流式输出时只重绘并高亮当前这条消息，长对话里不再随长度变卡。

## 0.1.24 — 2026-06-09

### 新增 / 改进
- **思考过程展示重做**：模型思考时，思考框是一个固定高度的「取景窗」，内容在窗内滚动、始终跟住最新一行，标题显示「思考中」；思考结束后自动收起、标题变为「思考过程」，可随时点开重看。支持三种状态——点顶栏在收起 ⇄ 展开间切换，半展开时底部「展开全部」可切到完全展开。展开、收起，以及开头那颗品牌水晶的移动，全程平滑动画（思考时水晶移到思考框旁，思考完滑回回答首行）。思考内容也完整支持 Markdown、数学公式与代码高亮渲染，与正文一致。
- **DeepSeek 也能显示花费**：DeepSeek 接口不返回费用，改为本地按官方价估算（区分缓存命中 / 未命中两档），现在用 DeepSeek 对话也能看到每轮与累计花费。
- **费用统一按人民币显示**：移除了货币单位 / 汇率设置项，花费一律按人民币显示（内置汇率约 6.78）；不足 1 分仍显示 `<¥0.01`，悬停查看精确价格（含美元原值）。
- **设置默认打开「常规」**：首次打开（或无历史记录）时停在「常规」页，更顺手；仍会记住你上次停留的分页。
- **侧栏长标题改为渐隐**：左侧对话标题过长时，右侧用透明渐隐收尾，替代原来的省略号「…」。

### 修复
- **流式输出时选字不再跳到顶部**：AI 正在回答时选中文字、拖到另一端，不再偶发地把选区甩到对话最前面。

## 0.1.23 — 2026-06-09

### 新增 / 改进
- **「常规」设置栏目**：新增「常规」分页（设置首项），集中放置——开机自启并常驻后台、自动检查更新开关、启动时恢复上次对话、以及重看使用引导的入口。
- **费用可按人民币显示**：「常规」里可把 token 花费从美元切换为人民币（汇率内置约 7.2，可自行修改）。
- **小额花费更清爽**：花费不足 1 分钱时只显示 `<¥0.01` / `<$0.01`，鼠标悬停才展开真正的精确价格（人民币会附带美元原值与汇率）。
- **设置内搜索**：设置左上角新增搜索框，按关键词（如「汇率」「代理」「字体」）快速过滤、跳转到对应栏目。
- **水晶默认改为白水晶**：默认强调色由紫水晶改为更中性的白水晶（更贴合黑白界面）；旧版用户自动迁移一次，之后仍可在 外观 → 强调色 里自由更换。
- **速答水晶跟随强调色**：快速提问浮条的加载水晶现在跟随所选强调色与明暗主题，与主程序保持一致（此前固定为黑白）。
- **已归档点击外部收起**：展开左下角「已归档」后，点击聊天区或其他任意位置会自动收起。

### 修复
- **加粗紧贴空格时不渲染**：模型输出 `** 文字**` 这类首尾贴着空格的加粗写法时，现在也能正确加粗（主聊天页与速答浮条均已修复）。
- **（macOS）检查更新时下载进度乱跳**：重复触发检查更新会同时开多条下载、进度条在两组百分比之间反复横跳——已加并发保护，仅保留一条下载，已下载完成则直接标记为可更新。

## 0.1.22 — 2026-06-08

### 新增 / 改进
- **从任意一轮分支**：每条消息下方（或右键）新增「分支 / 从这里分支」——把该轮及之前的全部上文复制到一条新对话，原对话保持不动，方便从某个节点另起一条探索。
- **系统托盘**（Windows / Linux）：新增托盘图标，右键可「打开 Quartz / 快速提问 / 退出」，左键点开窗口。开启「开机自启」后，关闭主窗口会缩进托盘而非退出，全局速答持续可用。
- **开机自启并常驻后台**：设置 → 快速提问 新增开关，登录后在后台启动（主窗口不弹出），让全局速答随时可用；之后点托盘 / 任务栏 / Dock 图标，或按打开主窗口的快捷键即可唤出窗口。
- **OpenRouter 模型列表更清爽**：每行只显示简写名，鼠标悬停才浮出完整模型编号。
- **打开时定位当前对话**：启动时把左侧正在进行的对话滚动到距顶部约 20% 处，更容易一眼看到。

### 修复
- **重复启动开出多个窗口 / 多个实例**：新增单实例锁，再次启动（包括重复点击图标、更新后过期的固定图标）会聚焦已开窗口，不再开新副本，也避免多进程争用同一份数据。
- **（Windows）提示气泡被窗口按钮遮挡**：右上角最小化 / 最大化 / 关闭按钮附近的悬浮提示不再被系统控件盖住。

## 0.1.21 — 2026-06-08

### 新增 / 改进
- **品牌水晶 + 强调色**：每条 AI 回答开头会带一颗水晶（在正文左侧、对齐首行）——回答生成时切面流光，完成后静止成宝石。可在 设置 → 外观 → **强调色** 里挑色：紫水晶 / 蓝晶 / 粉晶 / 黄水晶 / 茶晶 / 绿晶 / 白水晶 七种预设，或**自定义任意颜色**；旁边有一颗实时预览水晶，悬停色块即可预览。
- **使用引导**：首次启动会有一份翻页式上手卡（真实截图、深浅两套），介绍系统提示词、思考强度、速答浮条、⌘L 聚焦输入框、节点小地图等；之后可在 设置 → 关于 里随时重看。
- **重新生成不再清空后文**：对较早的回答点「重新回答」时，后面的对话会原位保留，不再被一并删除；旧回答仍可在 ‹ 1 / 2 › 之间切换（任意有多个版本的回答都能切换）。
- **关于页**：新增「重启更新」按钮（更新下载完成后一键重启安装）；并展示历史更新日志。
- **设置记住上次所在页面**：重新打开设置时回到上次停留的分页，不再每次都跳到「模型服务」。

### 修复
- 对话标题：开头是「你好 / 在吗」这类寒暄时，后续追问没能触发标题更新的问题已修复。

## 0.1.20 — 2026-06-08

### 新增 / 改进
- **回到底部按钮**：长回答时若向上翻看，右下角会浮出一个按钮，点一下平滑滚回最新内容。
- **对话标题随对话演进**：标题不再只看第一句话——每多问一个问题，AI 会判断当前标题是否还概括得了整段对话，必要时自动更新（手动改过的标题不会被覆盖）。判断只依据你的提问，不看回答。
- **节点小地图标题更聪明**：生成标签时会参考同一对话里已有的节点标题，像「继续」「再展开」这类追问也能得到有意义、且不重复的标签。
- **代码块显示完整语言名**：如 `JavaScript`、`Python`、`TypeScript`（原来只显示 `js` / `py`）。
- **模型名更简洁**：Google 模型自动去掉末尾的 `-preview`（`gemini-3-flash-preview` → `Gemini 3 Flash`）。
- **速答浮条**：加载图标（Quartz 水晶）更醒目；移除右侧多余的模型名。
- 输入框占位提示精简。

### 修复
- 修复中文里**加粗 / 斜体紧贴标点时不渲染**的问题（如 `**深度学习（注）**` 不再漏出星号），主聊天页与速答浮条均已修复。

### 其他
- **全新安装不再预置模型**：改为由你自行添加；添加的第一个模型会自动设为默认对话 / 命名模型。
- **数据目录更名为 Quartz**：从旧的 `ChatBox` 目录自动迁移，数据无损。
- **卸载程序可选删除数据**（Windows）：卸载时可选择是否一并删除对话、设置、API Key 等数据。

## 0.1.19 — 2026-06-08
- **模型显示名自动生成**：按各家命名规则生成简洁名称，不再被截断。
- **本地代理**：填上端口即可走 Clash 等本地代理，并支持连接测试。
- **采样参数**：新增 top_p / top_k。
- **高对比代码配色**，关于页加入功能列表。
- **Windows 打磨**：新对话上移、设置打开时窗口控件随之变灰、快捷键改为 Alt+Space / Alt+Shift+L（含迁移）。
- 修复：填完 API Key 后空主页不刷新、「已更新标题」提示未居中等。

## 0.1.18 — 2026-06-07
- **自动滚动锁定**：回答流式输出时自动跟随，提问停在距顶约 20% 处。
- 真·iOS 风格开关、关于页自动检查更新。
- 修复：深色模式输入框渐隐、设置文本框自动增高、提示文案清理。

## 0.1.17 — 2026-06-07
- 设置页重新设计、模型名简化、Windows 快捷键与安装器修复。

## 0.1.16 — 2026-06-07
- 右键菜单、滚动修复、彩色代码高亮，以及一批细节打磨。

## 0.1.15 — 2026-06-07
- 输入区重新设计，更友好的「日常」默认提示词。

## 0.1.14 — 2026-06-07
- 模型服务重做：各家独立配置 Key 与模型，支持从 API 拉取模型列表。

## 0.1.13 — 2026-06-07
- 速答浮条支持公式渲染，加入旋转水晶加载动画。

## 0.1.5 – 0.1.12 — 2026-06-07
早期版本的基础打磨：首个 macOS 通用包（Intel + Apple Silicon）与 Windows 安装器、窗口状态记忆、自动备份、错误分类与重试、按对话增量存储与长对话虚拟化、去除钥匙串弹窗、侧栏更新提示、每日检查更新等。
